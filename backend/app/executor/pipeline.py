"""Pipeline — placeholder para hooks (validação extra, métricas, auditoria).

No MVP, o pipeline está embutido em Executor.executar(). Esta camada existe
apenas como ponto de extensão futuro, sem código (§13).
"""
